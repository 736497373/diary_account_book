import 'package:flutter/material.dart';

class HomeCurrentBudget extends StatelessWidget {
  const HomeCurrentBudget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 150,
      color: Colors.deepOrange,
      child: null,
    );
  }
}
